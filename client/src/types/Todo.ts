export interface Todo {
  todoId: string
  createdAt: string
  name: string
  partyName: string
  dueDate: string
  inviteDate: string
  address: string
  wish: string
  done: boolean
  attachmentUrl?: string
}
